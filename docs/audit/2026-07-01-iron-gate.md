# Security Audit · Iron Gate · 2026-07-01

| Поле | Значение |
|------|----------|
| Статус | PASSED |
| Прогон | iron-gate |
| Уровень | full |
| Охват | leaks + code |
| Модель | Claude Opus 4.8 |
| Дата | 2026-07-01 |

> Повторный прогон после устранения находок из [2026-07-01-first-pass.md](2026-07-01-first-pass.md). Обе Medium path-traversal закрыты (adversarial-проверка: обход не найден, обратная совместимость цела). `.gitignore` и CI-hardening применены.

## Сводка

```
Трек A · Секреты/ключи:        0  (Crit 0 / High 0)
Трек A · PII/экспозиция:        0
Трек A · История git (11):      0
Трек B · Инъекции/exec:         0
Трек B · Path traversal:        0  (закрыто)
Зависимости/supply chain:       0
Инфра/CI:                       0

Severity: Crit 0 · High 0 · Med 0 · Low 1 · Info 4
Вердикт: PASSED
```

Гейт: Critical/High/Medium = 0 → **PASSED**.

## Что закрыто с прошлого прогона

| Было (first-pass) | Статус | Как закрыто |
|-------------------|--------|-------------|
| Medium · path traversal через `dir` из конфига | **закрыто** | Нормализация целевого пути (`realpath`/`GetFullPath`) + строгая проверка вхождения в границу (`$HOME` для install, `$TargetRoot` для sync) до любого удаления; отклонение `..`/ведущего `/`/`:`/абсолюта. Все 4 установщика. |
| Medium · path traversal через имя скилла из CLI | **закрыто** | Whitelist `^[A-Za-z0-9._-]+$` + подстраховка `within(dst, target_dir)`: имя `..` схлопывается и отбивается проверкой границы. |
| Low · gitignore не покрывает секреты | **закрыто** | В `.gitignore` добавлены `.env*`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `secrets*.json`, `credentials*.json`, `service-account*.json`. |
| Low · `actions/checkout@v4` по тегу | **закрыто** | Запиннен по SHA `34e11487…` (# v4). |
| Info · нет `permissions:` в CI | **закрыто** | Добавлено `permissions: contents: read`. |

## Открытые находки

| Severity | Категория | Файл:строка | Описание | Рекомендация |
|----------|-----------|-------------|----------|--------------|
| Low | dead code | `scripts/install.sh` (функция `install_skill`) | Неиспользуемая функция с `rm -rf "$dst"` без проверки границы. Не на пути исполнения (установка идёт через Python-heredoc), поэтому не эксплуатируется, но вводит в заблуждение. | Удалить мёртвую функцию. |
| Info | regex hardening | установщики | Whitelist имени скилла допускает точки (`..` проходит регекс), но traversal ловится проверкой границы `within`. | Опционально запретить имена, начинающиеся с `.`. |
| Info | supply chain | репозиторий | Внешних зависимостей нет, `curl\|bash` нет, поверхность ~0. | - |
| Info | injections | установщики | Инъекций/`eval`/`sh -c` нет; переменные квотированы, JSON через `json.load`. | - |
| Info | secrets/PII | дерево + история git (11) | Секретов, ключей, PII не найдено; история чистая. | - |

## Итог

Репозиторий готов к деплою и смене видимости на public: Critical/High/Medium = 0. Остаток - один Low (мёртвая функция) и Info-наблюдения, деплой не блокируют.

Значения секретов в отчёт не выводятся - найденных секретов нет.
